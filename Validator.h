#ifndef VALIDATOR_H
#define VALIDATOR_H

#include <string>

class Validator {
public:
  static int validateAndGetInt(const std::string &prompt);

private:
  // Add other validation functions if needed
};

#endif // VALIDATOR_H
